{% extends 'base.html.twig' %}

{% block title %}Albums{% endblock %}

{% block body %}
<div class="text-center">
    <h1>Albums</h1>
    <p>Voici les albums de notre équipe.</p>

</div>

<section class="album-display">
    {% for album in albums %}
    <div class="card">
        <div class="card-info">
            {# <img class="card-avatar" src="images/posts/{{album.image_id}}"> #}
        </div>
        <div class="card-title">{{album.name}}</div>
        {# <div class="card-subtitle">{{album.date}}</div> #}
    </div>
    {% endfor %}

{% endblock %}
